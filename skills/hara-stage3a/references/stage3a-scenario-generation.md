# Stage 3A：场景与危害事件生成方法

本文件只定义 Stage3A 的语义生成方法，不定义 JSON schema 或上下文拆分流程。输出结构见 `json-contracts.md`，上下文生成见 `SKILL.md`。

## 输入边界

只使用当前 `stage3_context_<MF_ID>.json` 中的信息：

- `mf`：当前 Stage2 MF 行。
- `hazard_reasoning`：Stage2 的危害映射推理。
- `function_context` 或 `matched_functions`：由 Stage1 context 复用来的功能背景。
- `operating_domain_hints`：从功能背景提取的运行域提示。

不要读取完整 Stage0、完整 Stage2、风险评估知识库或安全目标知识库。

## 生成目标

- 每个 MF 生成 10-20 条真实可信场景。
- 只生成场景、危害事件、`scenario_reasoning` 和 `max_asil_planning`。
- 不生成 S/E/C、ASIL、安全目标或安全状态。
- 场景应覆盖可信最高风险路径，而不是机械枚举道路、天气和车速组合。
- 每条场景必须形成闭环：`当前 MF 故障 -> 整车危害 -> 运行场景中的危险输出/异常状态 -> 风险对象受伤风险`。如果移除当前 MF 故障后，同一危害事件仍会由天气、路面、交通或驾驶员行为自然发生，则该场景不是当前 MF 的有效 HARA 场景。

## 最大风险路径规划

生成场景前必须先写 `max_asil_planning`。规划时按三类风险驱动因子思考：

| 方向 | 要问的问题 |
|---|---|
| 可能提高 S | 故障如何造成更严重的人身伤害？风险对象是谁？伤害能量从哪里来？ |
| 可能提高 E | 该场景是否处于功能真实运行域？用户是否经常遇到？ |
| 可能提高 C | 异常是否难以及时感知？驾驶员或他人是否有足够时间和空间控制？ |

`max_asil_planning` 至少包含：

- 高风险因素分析
- 规划的场景原型
- 预期最大_ASIL（规划假设，不是最终评级）
- 规划理由

## 功能运行域

优先从 `function_context.function.detail_text`、`operating_domain_hints` 和 MF 描述中提取；只有旧数据 fallback 没有 `function_context` 时，才使用 `matched_functions.detail_text`：

- 触发源：驾驶员请求、自动触发、系统条件触发。
- 运行边界：车速、车辆状态、道路/环境前置条件。
- 预期输出：制动、转向、驱动、换挡、驻车、照明、警示、门锁、能量管理等。
- 排除条件：功能明确不工作或不应输出的状态。

请求相关故障通常必须发生在功能运行域内；只有“非预期激活、无请求输出、条件不满足仍输出”等故障可以考虑运行域外危险输出，并必须解释原因。

## 场景原型

优先生成这些原型：

- 可信最高风险原型：最可能形成最高风险的真实组合。
- 接近最高风险原型：改变一个关键因子，观察风险边界。
- 常见高暴露原型：高频但不一定最严重的运行场景。
- 可控性边界原型：驾驶员在环/不在环、能感知/难感知、有空间/无空间。
- 低风险或不适用边界原型：用于说明风险上限，数量要少。

同一核心风险机制最多保留 2 条，不要只替换天气、道路名或地标制造重复场景。

## 场景字段

六大场景字段必须逐字来自 `operation_scenarios.json` 对应枚举：

- `道路类型`
- `道路条件`
- `环境条件`
- `车辆状态`
- `车速(km/h)`
- `特殊要素`

库外细节只能写入 `附加条件`。只有与场景中其他条件语义或物理上互斥的字段（如城市支路与维修车间），才将矛盾方的次要条件写为”不涉及”。不能仅因为某字段不影响危害事件因果链就标记为”不涉及”——它可能对后续 SEC 评级有影响，应在相关性检查中如实说明其与危害事件的因果关系及对 SEC 的潜在影响。

### 场景条件相关性判定

`场景条件相关性检查` 必须逐项说明六大字段与当前场景的关系：

- **相关**：该字段参与了故障到危害事件的因果链，或影响了风险对象位置、危险路径、碰撞能量等关键因素。写”相关，理由”。
- **不涉及**：仅用于以下情况：
  1. 该字段与场景中其他已选字段在语义或物理上互斥（如城市支路与维修车间），将矛盾方的次要条件标为”不涉及，与XX条件矛盾”。
  2. 该字段对当前 MF 故障的因果链和 SEC 评级均无任何影响。
- **不影响危害事件但影响 SEC**：该字段不参与故障到危害事件的因果链，但会影响后续 SEC 评级（如天气影响暴露频率 E、夜间影响可控性 C、高速影响严重度 S）。写”相关，不影响危害事件因果链，但影响SEC的X评级，理由”。
- 天气、能见度、玻璃起雾、路灯、强光等字段，只有在当前 MF 是感知/照明/传感/驾驶员视野相关故障，或它们直接改变车辆危险输出时，才可标为危害事件因果相关。否则按”不影响危害事件但影响 SEC”处理。
- 对 EPB/驻车保持/夹紧力不足/拉起无响应等驻车类故障，冰雹、降雨、雾霾、玻璃起雾、冰雪路面不能单独构成溜车因果。必须另有坡度、驱动扭矩、未挂 P 挡、外力推动等实际纵向力；否则应剔除该场景。

## 危害事件

写 `危害事件` 前必须确认：

1. 车辆实际危险输出或异常状态。
2. 风险对象类型和位置。
3. 风险对象是否位于危险路径上。
4. 道路、车辆状态、车速和驾驶员是否在车上是否逻辑一致。
5. 当前 MF 故障是否是该危害事件的必要原因。
6. 风险对象名称是否与碰撞对象一致：前方停放车辆/前车对应“前车乘员/本车乘员”，对向来车对应“对向车辆乘员”，行人对应“行人”。不要把“前车”场景写成“对向车辆乘员”受害。

危害事件建议格式：

```text
<风险对象>在<场景条件下>因<车辆异常状态或危险输出>受到<伤害类型>风险
```

涉及坡道溜车、行人风险、追尾/被追尾时，必须同时遵循 `vehicle-dynamics-rules.md`。

## 推理记录

每个场景必须包含 `scenario_reasoning`：

- `场景规划理由`：说明覆盖了哪个风险原型。
- `危害事件推理`：说明故障、危害、场景如何推出危害事件；必须出现故障导致的异常输出或异常状态，不能只描述天气、路面或障碍物本身。
- `场景条件相关性检查`：逐项说明六大场景字段相关或不涉及的理由。

## 筛除规则

生成前和生成后都要剔除：

- 不在功能运行域内的请求相关故障场景。
- 为提高风险硬拼的极端条件。
- 移除当前 MF 故障后仍会发生同一危害事件的场景。
- 把天气、低附着路面、视野受阻当作 EPB 驻车溜车唯一原因的场景。
- `道路条件` 选择 `直道`，但 `附加条件`、危害事件或推理中又写下坡/上坡/坡度/陡坡的场景。
- 只替换枚举值但风险机制相同的重复场景。
- 未通过下方「场景后一致性校验流程」的场景。

## 场景后一致性校验流程

场景生成完成后、输出之前，对每条场景执行以下两步校验。发现问题时优先修复；无法修复的剔除该场景。

### 第一步：条件间互斥校验

逐一检查场景中所有已填写条件（六大字段 + 附加条件）之间是否能同时存在于同一个真实物理场景中。判断准则：两个条件在语义和物理上是否互斥。

处理原则：
- 保留与当前 MF 故障到危害事件因果链最直接相关的条件。
- 将矛盾方的字段改为"不涉及"，在 `场景条件相关性检查` 中写明与哪个条件矛盾、为何改为不涉及。

### 第二步：条件到危害事件的物理一致性校验

从场景条件出发推导车辆实际行为，与危害事件描述逐项比对：

1. **运动方向推导**：根据道路条件、车辆状态和故障模式，确定车辆故障后的实际运动方向。
2. **风险对象位置校验**：根据车辆实际运动方向，验证风险对象是否处于该方向的危险路径上。运动方向与风险对象方位矛盾的危害事件必须修正。
3. **伤害机制校验**：危害事件描述的碰撞方向、碰撞对象和伤害类型是否与车辆实际运动一致。

不一致时，修正危害事件使其与场景条件物理一致；无法调和的剔除该场景。

验证命令以 `SKILL.md` 为准。
